services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: bookkeeper
      POSTGRES_PASSWORD: bookkeeper
      POSTGRES_DB: bookkeeper
    ports:
      - "5432:5432"
    volumes:
      - bookkeeper_pgdata:/var/lib/postgresql/data

volumes:
  bookkeeper_pgdata:
